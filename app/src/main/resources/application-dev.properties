spring.datasource.url=jdbc:postgresql://localhost:5432/cestajusta
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

jwt.secret-base64=${JWT_SECRET:7WR/z7mNy/4SMGg0fmc5i79b5EX8BsTJGaAASswJUio=}

server.port=8080

logging.level.org.furb.security=DEBUG
logging.level.org.springframework.security=DEBUG

# Seed data (runs after Hibernate creates the schema)
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:cestajusta}
spring.rabbitmq.password=${RABBITMQ_PASS:cestajusta}
spring.rabbitmq.virtual-host=${RABBITMQ_VHOST:cestajusta_dev}
spring.rabbitmq.publisher-confirm-type=correlated
spring.rabbitmq.template.mandatory=true

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB

app.uploads.dir=${APP_UPLOADS_DIR:./data/uploads}
app.outbox.relay-delay-ms=${APP_OUTBOX_RELAY_DELAY_MS:10000}
